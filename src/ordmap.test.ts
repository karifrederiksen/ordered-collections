import { expect } from "chai"
import * as Jsv from "jsverify"
import { OrdMap } from "./ordmap"

describe("OrdMap", () => {
    describe(".toArray()", () => {
        it("should return the map as an array of tuples, ordered from min to max", () => {
            //
        })
    })
})
